import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import primeraseccion from './primero.1';

if (document.getElementById('primeraseccion')) {
    ReactDOM.render(<primeraseccion />, document.getElementById('primeraseccion'));
}